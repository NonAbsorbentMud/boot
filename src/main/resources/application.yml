spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test?useUnicode=true&characterEncoding=utf-8
    username: root
    password: mysql
  profiles:
    active: dev
mybatis:
  mapper-locations: classpath:own/cfb/boot/mapper/*.xml
# config-location: classpath:own/cfb/boot/mybatis-config.xml